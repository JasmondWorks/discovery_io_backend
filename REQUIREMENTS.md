search the web for users that have used it and get the success rankings. Calculate the average. Search the web each time user searches or every x interval and the save the ranking value for that tool into the db

or to seed the database, search the internet to get ranking for each tool

for a particular search query, one or multiple tools are returned

after onboarding, redirect them to an optional tools catalog section (can be skipped)

tools catalog: a library of tools (shows the same thing for all users) (part of the primary navigation links)
